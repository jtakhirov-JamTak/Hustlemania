# PROGRESS

Shipped milestones and the metric log.

**This is not a handoff file.** In-flight session state has one owner:
`session-context.md`, written by `/save-context` and read by `/resume-context`. Do not
duplicate next-actions or working state here.

## Metric log

The governing metric is time from request to a correct, green, usable feature. One row
per SPEC feature. Times are UTC (`date -u +%Y-%m-%dT%H:%MZ`, or in PowerShell
`(Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mmZ")`) — read the clock, never
estimate it.

**A row is opened when the work starts and closed when it is verified green. It is
never written in one go after the fact**, because a row reconstructed at the end
measures nothing but memory.

| Feature | started_at | green_at | cycle_time | human_stops | rework | defects_after_green |
|---|---|---|---|---|---|---|
| F1 — Walking skeleton: sign in → Vision → start Sprint → Today → Close Day | 2026-09-05T04:15Z | 2026-09-05T17:27Z | 13h12m | 2 (Docker Desktop not running before `supabase start`; commit-scope approval for the `mockup/today` branch and the fast-forward to main) | 0 | 0 |
| F2 — Cue and Impediment libraries; Highest Impediment; Day Close selections | 2026-09-05T18:05Z | 2026-09-05T19:16Z | 1h11m | 0 | 0 | 0 |
| F3 — Custom daily targets; reconciliation; target locking; intention pre-planning | 2026-09-05T19:25Z | 2026-09-05T19:55Z | 0h30m | 0 | 0 | 0 |
| F4 — Tasks: per-day optional task list, locks with the day, no roll-over | 2026-09-05T20:08Z | 2026-09-05T20:46Z | 0h38m | 0 | 0 | 0 |
| F5 — Day boundaries, streaks, missed days, backfill | 2026-09-05T20:56Z | 2026-09-05T21:33Z | 0h37m | 2 (both to remove an unapplied `0007` draft so it could be rewritten — the write and shell guards block any change to a migration file once it exists) | 1 (first 0007 draft rebuilt the immutability trigger from its 0001 body and lost 0004's snapshot columns; caught by the existing DB suite before commit) | 0 |
| Audit remediation — buckets A + B of `docs/audits/full-audit-2026-09-05.md` (not a SPEC feature; user-approved fix pass; #43 wizard split and bucket D to BACKLOG) | 2026-09-06T00:51Z | 2026-09-06T02:14Z (phases 1–3 green at 01:14Z, user stopped there and resumed; 4–6 green at 02:14Z: verify hooks 115 / unit 54 / DB 178 / e2e 8, build green, phone and desktop captures inspected) | 1h23m | 1 (user: "stop after phase 3 completes") | 0 | 0 |
| Enabling pass — shell inline styles to classes, Tailwind dropped, no visible change (not a SPEC feature; direct build approved 2026-09-06 in `docs/RECONCILIATION-2026-09-06.md`; scope narrowed to the shell on the user's call) | 2026-09-07T05:11Z | 2026-09-07T05:39Z (typecheck, lint, build without PostCSS, verify: hooks / unit / DB / golden path 8 green on desktop + phone; 22 full-page captures pixel-identical to the pre-change baseline, check proven to fail on a 1px mutation; the temp capture spec's phone dialog shot is scroll-position fragile inside the full suite and is deleted) | 0h28m | 1 (scope narrowed to the shell on the user's call after the 372-style inventory) | 0 | 0 |
| F6 — Libraries v2: cue trigger, RECOVERED WHEN, relabelled editors | 2026-09-07T17:17Z | 2026-09-07T18:33Z (0009 applied by `db reset`; DB 191 incl. the F6 block and the pin test, six live mutations each red then restored green; verify: hooks 115 / unit 54 / DB 191 / e2e 8 on desktop + phone; Chrome desktop check of Cues, Impediments (view, Edit, rule-22 rejection) and the Today card against the v8 Libraries artboard; phone rests on the Playwright phone project) | 1h16m | 1 (user: after the spec review and the gate, build deferred to the next session) | 0 | 0 |
| F7 — Day observations: what showed up, what was used, did the response run | 2026-09-07T18:48Z | 2026-09-07T20:18Z (0010 applied by `db reset` twice, the second for the blank start; DB 204 incl. the three F7 blocks, the guard test and the pin markers; eight live mutations each red then restored green; verify: hooks 115 / unit 54 / DB 204 / e2e 8 on desktop + phone; Chrome desktop check of the Today cue rows and the close dialog's two steps against the v8 artboard, the Chrome close wrote the expected five rows; eval-04: no P0/P1, three P2 gaps fixed before green, the harness P2 to BACKLOG; phone rests on the Playwright phone project) | 1h30m | 0 | 1 (eval-04 found the backfill e2e and the information_schema test short of their SPEC lines, plus one copy departure; fixed and re-run before green) | 0 |
| F8 — Journal restyle: timeline, rail, Dusk, night mode | 2026-09-07T23:11Z | 2026-09-08T00:05Z (no migration; unit 60 incl. the six daySummary tests, DB 204 unchanged, e2e 8 on desktop + phone rewritten around the journal with the pins moved (64/16/30, result 78 on backfill only) and the night-mode cookie round trip; verify green: hooks 115 / unit 60 / DB 204 / e2e 8; four live mutations each red then restored (recovery word dropped, quietItems offering the highest, the highest-first tail, the theme action ignoring the mode); Chrome desktop check of Day 9 in Dusk and Night against the v8 artboard: folds, closed and missed rows, the inline close through Confirm close, the Closed card with Set up tomorrow, the toggle both ways; phone rests on the Playwright phone project) | 0h54m | 0 | 1 (the phone capture showed the brand overlapping the Sprints tab once the toggle was in; fixed before green) | 0 |
| F9 — Vision v2: one vision, three annual steps, obstacle link, dated reviews | 2026-09-08T00:16Z | 2026-09-08T03:38Z (0011 applied by `db reset`; DB 224 incl. the 20-test F9 file: the collapse run from the migration's own marked block with a flipped-order companion, RLS-disabled leak check on `vision_reviews`, the five functions, the shared-row trigger case and the obstacle guard; four live function mutations each red then restored green; verify: hooks 60/21/23 / unit 64 / DB 224 / e2e 8 on desktop + phone; Chrome desktop check of the overview with the review card, step 2 and Night step 3 against the f9-vision captures, pick-clears-name exercised, wizard step 1 with zero inline styles; eval-05: no P0/P1, two P2 fixed before green, observations to BACKLOG; phone rests on the Playwright phone project incl. overflow checks on every Vision view and wizard step) | 3h22m | 1 (user: fresh session for the build after the gate) | 1 (the first e2e run found the step-2 create path dropping `p_impediment_id` — FIX_LOG 2026-09-07; eval-05's two P2s — the deadline `min` vs UTC and the `no_active_vision` copy — fixed and re-run before green) | 0 |
| F10 — Sprint completion, End Early, postmortem, kit, next-sprint gate | 2026-09-08T04:17Z | 2026-09-08T07:44Z (0012–0015 applied by `db reset`; DB 270 incl. `completion.test.ts` 34 — the three closure windows, the transition trigger, the cancellation predicate, rule 26, ten `finish_review` rejections with atomicity, RLS on both new tables with the disable/enable check, and the F5 streak defect closed — and `insights.test.ts` 12 on a hand-computed 14-day fixture carrying a zero-target day and a cancelled day; unit 84 incl. `insightCards.test.ts` 16 and the celebration thresholds; e2e 10 on desktop + phone, the F10 walk running finish → gate → postmortem → finish review → kit pre-fill with DB assertions and zero overflow at 390px; ten live mutations each turned a named test red and were restored from disk; Chrome desktop check of the Finish card, the gate, the postmortem and the rail in Dusk and Night; eval-06: no P0/P1, four of the eight P2s fixed before green, three were wrong criteria and the SPEC is amended, one to BACKLOG) | 3h27m | 1 (mid-build the user asked where F9 and F10 stood; work resumed on their go-ahead) | 9 (all found and fixed before green: the implicit PUBLIC execute grant on ten functions, caught by the grants pin — FIX_LOG · the `Reviewed` stamp a day ahead of the sidebar's, caught in the Chrome check — FIX_LOG · the Sprints sidebar reading `Ready` for an unreviewed area, caught in the Chrome check · the `.across` styles never added, so the stat row stacked · and five from eval-06: the missing Across card, `partially` left out of `answered`, `sprint_best_streak` disagreeing with `sprint_streak_at`, the lesson card not naming its Area, and the celebration's missing 80% state) | 0 |
| F11 — Insights v2: Across sprints, Suggested kit, Insights sidebar | 2026-09-09T05:35Z | 2026-09-09T07:09Z (no migration — the cross-sprint numbers are F10's per-sprint functions fanned out, so DB stays 270; unit 84 → 114 (`across.test.ts` 30); e2e 10 on desktop + phone with the F10 walk extended through the Across page, the per-chip empty state and a junk `?scope=`; seven live mutations each turned a named test red and were restored from disk; visual verification on real seeded history at 1440px and 390px in Dusk and Night via the Playwright projects — Chrome caps at 1138px here — with the footer's two-column layout asserted rather than eyeballed; evaluator trigger none: no migration, no new function, no grant or policy change; `npm run verify` green: hooks 60/21+23/6+5, unit 114, DB 270, e2e 10) | 1h34m | 1 (the feature gate) | 0 | 4 (all found and fixed before green: the All-areas rows carried no Area label, so two "Starting late" rows were indistinguishable — the approved decision had named the label and I had not built it · the cue card sorted "Not enough data" above its +45 pts rows, because `rank()` parks thin rows at 999 and I sorted descending · "most recent sprint" resolved by array order rather than `end_date`, so a card quoted Jul 31 while a qualifying Aug 20 sprint existed · the e2e's Suggested-kit assertion was a loose regex that passed by matching `Keep ` while my belief about the fixture was wrong) | 0 |
| F13 — Evening email reminder | 2026-09-10T02:04Z | 2026-09-10T03:13Z (0018 applied by `migration up`, then by `db reset` from scratch — pg_cron + pg_net enabled, `reminder_log`, three service-role-only functions, the hourly Vault-driven job; DB 277 → 293 incl. `reminders.test.ts` 15; unit 130 → 147 (`reminders.test.ts` 16, `zoneAtLocalHour` 1); e2e 12 on desktop + phone incl. `e2e/reminders.spec.ts` on desktop (bearer gate, one send, idempotent rerun, closed day); fourteen live mutations each turned a named test red and were restored — six TS, seven DB (grants, RLS, the 20:00 predicate, the sent filter, the claim window, the job) and the route's 401 branch under the e2e; visual verification n/a (no screen); evaluator `docs/evals/eval-07.md`: 0 P0, 0 P1, 2 P2 (SPEC text, fixed in place), working tree identical before / first / final; `npm run verify` green: hooks 60/21+23/6+5, unit 147, DB 293, e2e 12) | 1h09m | 1 (the feature gate; the four interview calls were one batch) | 0 | 0 |
| F14 — Pre-release: deploy, install check, reminders live, restore drill | 2026-09-10T03:34Z | 2026-09-10T06:20Z (migrations 0001–0018 pushed to the hosted project, eleven read-only assertions pass, `db diff --linked` clean but for the platform's own `ensure_rls`; auth config as code via `[remotes.production]` and three user-run `config push`es; app on `https://hustlemania.app` through the GitHub → Vercel integration, `www` 308 → apex; auth mail through Resend on that domain; owner signed in, `/sprints` screenshot on the origin; the 06:05 UTC job POSTed the route → `200 {due:1, users:1, sent:1}` and "Relationships · Day 1 is still open" landed from `sprint@hustlemania.app`; `e2e/deployed.spec.ts` 4/4 on the origin with two mutations red then restored; Install confirmed by the user; restore drill: `db dump --linked` → blank local stack → all eight counts identical → `/login` 200 → stack reset; two production defects found and fixed before green (FIX_LOG 2026-09-10 ×2: the email-provider switch, the clock-skew retry with 5 unit tests and two mutations red); eval-08: 0 P0, 0 P1, 2 P2 (BACKLOG; PROJECTS.md pending the user's word), working tree identical before / first / final; `npm run verify` green twice — the evaluator's run and mine: hooks 60/21+23/6+5, unit 152, DB 293, e2e 12 + 6 skipped) | 2h46m | 13 (release is a chain of user-side steps: the feature gate · the Vercel import · the push word · three config pushes run by the user · DNS at Cloudflare · the Resend key into `.env` · three Vault attempts before a 64-char bearer · two sign-in rounds · the Install confirmation · the backup and plan calls) | 3 (the first config push carried local test values into production, pinned back in the remotes block · `auth.email.enable_signup = false` switched email logins off · the Vault bearer held placeholder text twice; regenerated and set in both places) | 0 |
| U1 — Layout: chip-row sidebar on every width, full-width workspace, wider rail, proof point as rows | 2026-09-10T22:27Z | 2026-09-10T22:54Z (CSS + two layouts + the Rail's proof rows; e2e golden path amended: sidebar 1280px on desktop, `main` below it, the selected chip's sub line visible and an unselected chip's clipped on both viewports; visual verification in Chrome at 1535px on real seeded data — journal with the wider rail and the three proof rows, Health empty state, Vision, Insights — plus the key state of switching chips; phone rests on the Playwright `phone` project (computed-style + no-overflow assertions), Chrome will not resize below desktop here; evaluator trigger none: no migration, no auth, no data change; `npm run verify` green: hooks 60/21+23/6+5, unit 152, DB 293, e2e 12 + 6 skipped) | 27m (the session was cut off after the edits at 22:29Z; resumed 22:32Z) | 1 (the resume) | 2 (both red then fixed before green: the new clip assertion picked the first chip, which `/sprints` selects by redirect — scoped to `.side-link:not(.side-link-on)` · a latent phone overflow, FIX_LOG 2026-09-10) | 0 |

### Who opens the row

- **`/interview` opens it**, as its first workflow action, for any feature that goes
  through it — so the clock includes the feature's own planning, not just its code.
- **BUILD opens it** for a direct build that skipped `/interview` (the one-sentence
  reversible change), immediately before implementation begins.
- **New app:** `/interview` opens **F1** when the new-app interview begins. F1's
  `cycle_time` therefore covers request → interview → design → approval →
  fresh-session handoff → implementation → verification → green, which is the number
  worth knowing. F2 onward start when work on that feature starts.

**At most one row may be open at a time** (`green_at` = `—`). If a second feature is
about to start while a row is still open, the previous one never went green: resolve
that row first. If planning ends with nothing to build, delete the row it opened.

### Who closes the row

**BUILD closes it, always.** `green_at` is written only after all required and
available verification passes — acceptance checks, visual verification when UI changed,
the evaluator pass when a trigger applied. If a required check fails or is skipped, the
row stays open and records why. Tooling unavailability blocks green only when that
verification is required by the acceptance criteria or the release boundary. A
`green_at` ahead of its evidence is the one failure this log cannot survive.

Then record `cycle_time` = `green_at` − `started_at` as elapsed time (`1h40m`, `2d3h`).

### Columns

- **cycle_time** — the metric. Real elapsed time from the moment work began to
  verified green.
- **human_stops** — how many times the build loop stopped for the human. An ordinary
  feature should be 0; the BUILD section lists the only legitimate reasons. Anything
  above 0 carries its reason in the row.
- **rework** — how many times work already believed finished had to be reopened
  *before* green, evaluator P1 fixes included. The evaluator pass runs inside the
  feature's own loop, so a finding it raises there is rework, not an escaped defect.
- **defects_after_green** — the escaped-defect counter: defects found in use after
  `green_at` was written. A later defect does **not** reopen the row — `cycle_time`
  already happened and cannot be un-measured. It increments this column and gets a
  dated `docs/FIX_LOG.md` entry with its regression test.

A row that never gets filled in is itself the signal: the loop is not being followed,
and the metric cannot be read.

## Shipped milestones

(Newest first. One line per shipped milestone, dated.)

- 2026-09-09 — **Full review of F6–F11 and the F11 fixes** (`docs/audits/full-review-2026-09-09.md`;
  DECISIONS 2026-09-09). `/review-changes` on `9cd1325` found 2 HIGH / 2 MEDIUM in F11; the
  nine-reviewer `/full-review` over `db40dee..HEAD` found 2 CRITICAL (a member removed and
  re-added could never be reviewed; a mid-sprint highest change demanded a verdict the UI
  never offered — both reproduced live, both Area-locking) plus 9 HIGH and 16 MEDIUM
  groups left open for triage. Fixed: both CRITICALs (migration `0016`, `onePerItem`), the
  four F11 findings (recovery vote from the SQL rate, kit pairs recovery by item + Area,
  postmortem coverage over effective days, touch targets on any coarse pointer). Unit
  114 → 121, DB 270 → 274; seven live mutations each turned a named test red. **Second
  pass, same day, on the user's call: every open HIGH and MEDIUM (#5–#29) fixed** —
  migration `0017` (`sprint_totals` view, five `*_many` wrappers, rule-26 exemption for a
  sprint that never ran, removed members default to drop), the 1000-row cap named in
  code, the Insights page at five requests for any N, focus and announcement after every
  in-place replacement, 44px on every primary control, the Dusk contrast misses, the
  device date read after mount, the theme redirect guard, session checks on direct-table
  actions, the operator-log context filter. Unit 121 → 130, DB 274 → 277, e2e 10; seven
  more mutations red; Chrome desktop check of the wizard, Across, a postmortem, the
  Journal rail and the Vision overview. Two design calls deferred to BACKLOG; LOWs open.

- 2026-09-05 — **F5 streaks green** (no evaluator trigger; nine DB mutations each turned
  their tests red, six more after the review rewrite; visual check in Chrome;
  `/full-review` 0 CRITICAL / 0 HIGH / 8 MEDIUM all fixed; `npm run verify` green:
  unit 45, DB 169, Playwright 8). Migration `0007_streaks.sql` — `closed_on_time`
  written by `close_day`, which returns the streak; `sprint_streaks()` for the sidebar
  and Today in one read; backfill of missed days that counts but never repairs — and
  the streak under Day N / 14, in the sidebar and on the result screen, with the whole
  missed plan-grid cell as the Backfill button.
- 2026-09-05 — **F4 tasks green** (eval-03: 5/5 criteria PASS, 0 P0/P1, 3 P2 → BACKLOG;
  eight DB mutations each turned their tests red; visual check in Chrome; `npm run verify`
  green: unit 43, DB 147, Playwright 6). Migration `0006_tasks.sql` — `tasks` table with
  RLS, column grants, archive-not-delete, `tasks_lock_with_day` trigger — and the Tasks
  card on Today with autosave rows, blank row offered, locked with the closed day.
- 2026-09-05 — **F3 custom targets green** (no evaluator trigger; six DB mutations and one UI
  mutation each turned their test red; visual check in Chrome; `npm run verify` green:
  unit 43, DB 120, Playwright 6). Migration `0005_targets.sql`, `save_targets`, the
  rule-10 row trigger, wizard custom mode with per-day intentions, Today plan card.
- 2026-09-05 — **F2 libraries green** (eval-02: 10/10 criteria PASS, 0 P0/P1, 3 P2 →
  BACKLOG). Cue and Impediment libraries with rank/scope/archive/restore, sprint
  membership (date-ranged, 1–3 / 1–5), Highest Impediment with WHEN → THEN, two-step
  Day Close with hurt/helped selections; migration 0004 with RLS, seven live mutations
  each turning a test red; verify = typecheck, lint, 19 unit, 94 DB, 6 e2e.
- 2026-09-05 — **F1 walking skeleton green** (eval-01: 14/14 criteria PASS, 0 P0/P1,
  6 P2 → BACKLOG). Magic-link sign-in, Vision, sprint wizard, Today, Close Day, on
  three migrations with RLS + deny-by-default grants; verify = typecheck, lint, 19
  unit, 51 DB, 6 e2e.
| U2 — Suggested kit: recovery figure from the same sprint as the follow-through, and recovery when the response ran | 2026-09-11T04:17Z | 2026-09-11T04:25Z (no migration, no UI change beyond the sentence text; `lib/acrossCards.ts` matches the recovery group on `from.id` and prints `with_recovered / with_response` at `with_response ≥ 3`; four unit tests red on the old code — 60→67, 50→0, an older sprint's 50 dropped, a ran-twice 75 dropped — then green; typecheck, lint, unit 155; evaluator trigger none; SPEC F11 kit wording corrected, FIX_LOG 2026-09-11) | 8m | 0 | 0 | 1 (closed on unit + typecheck + lint only; the full verify at 04:33Z found the F10 e2e asserting the old sentence — its response ran 2 of 4 times, below the new 3-occurrence bar, so the clause is rightly gone; expectation updated, `not.toContainText("recovers")` added) |
| U3 — CI: typecheck, lint, hooks and unit on every push and pull request | 2026-09-11T04:25Z | 2026-09-11T04:27Z (`.github/workflows/ci.yml`, one job on push to main and pull requests: `npm ci`, then the same four commands the local gate runs first, in its order; YAML parsed with js-yaml; the four steps green locally; DB and e2e stay in the pre-commit hook and a commented `verify-full` job says what unblocks them; NOT yet run on GitHub — the first run needs the push, which only the human authorizes) | 2m | 0 | 0 | 0 |
| U4 — README: what Hustlemania is and how to run it; the template README moves to docs/ | 2026-09-11T04:27Z | 2026-09-11T04:28Z (`git mv README.md docs/TEMPLATE_README.md`, new 55-line README; every path and command in it checked against the tree and package.json; one claim corrected before green — local sign-in needs a created user, there is no self-signup; no code, no tests, no evaluator trigger) | 1m | 0 | 1 (the sign-in line) | 0 |
| U5 — Skew retry actually retries inside a server render (memoized fetch returned the first 401) | 2026-09-11T05:06Z | 2026-09-11T05:16Z (`lib/supabase/skew.ts` marks the retry with `x-skew-retry: 1` so Next's dedupe-fetch key differs; three unit tests red on the old code — the memoised refusal, the preserved bearer, the marker — then green; `npm run verify` green: hooks 60/21+23/6+5, unit 157, DB 293, e2e 12 + 6 skipped; no skew 401 occurred in that run, so the live retry path is proven by the fake-dedupe tests only; evaluator trigger none) | 10m | 0 | 1 (a tuple type in the first assertion) | 0 |
| F15 — Situations: one response covers many situations | 2026-09-11T16:05Z | 2026-09-12T01:34Z (migration 0019: five new tables with RLS in the same file, in-place conversion of every user's rows with an end-of-file assertion, fourteen functions rebuilt from their named definers; `db reset` clean; `scripts/rehearse-0019.mjs` ten assertions green on legacy-shaped rows; ten live mutations, nine red and restored, the tenth — the view's `not cancelled` filter — unobservable because a check constraint already forbids a closed cancelled day; visual pass by Playwright (Chrome extension not connected): 48 captures, Dusk + Night × 1138 px + 390 px, no overflow, editor / blocked card / situations pages / wizard step 4 / close with per-situation recovery / breakdown lines all as planned; eval-09: 0 P0, 0 P1, 2 P2 → BACKLOG, working tree identical before / first / final; `npm run verify` green from a fresh reset: hooks 60/21+23/6+5, unit 167, DB 313, e2e 12 + 6 skipped. Hosted release 2026-09-12: `db dump --linked` to the user's backup folder (data 44 KB), the gate shown with the migration and `scripts/delete-user-rows.sql`, the user chose to push, `db push --linked` run by the user, `migration list --linked` 0019 remote, `db diff --linked` clean but for the platform's `ensure_rls`, deployed spec 4/4; the dump rehearsal did not run before the push — the classifier refused the load and the user chose speed, production having already received the code (FIX_LOG 2026-09-12); then the owner chose a full wipe instead of the per-user script: one TRUNCATE of all nineteen app tables run by the owner in the SQL editor, accounts kept, the pre-0019 dump the only copy — DECISIONS 2026-09-12) | 9h29m | 8 (four interview batches · the direction gate · the plan approval · two `! rm` rounds for the blocked draft) | 2 (the first 0019 draft failed reset on an auto-named CHECK · the conversion tripped the rule-17 immutability trigger, FIX_LOG 2026-09-11) | 0 |
| F16 — Vision v3: picture the year, the one-year goal with confidence, the obstacle plan; dictation on every setup box | 2026-09-12T05:41Z (the clock read when plan mode opened; the row was written at 06:13Z on plan approval because plan mode forbids edits) | 2026-09-12T07:15Z (migration 0020: three new columns, body and deadline nullable, `meaning` / `baseline` / `impediments.explanation` dropped at the owner's call, three step functions replacing three, `start_sprint` gated on all three steps; `db reset` clean; `scripts/rehearse-0020.mjs` eleven assertions green on legacy-shaped rows; seven live mutations red and restored (the sixth caught by the seed helper's insert, not the pin it was aimed at — the driver first misread a run in which no test executed); visual pass by Playwright: 20 captures, Dusk + Night × 1138 px + 390 px, no overflow; Chrome: the Dictate button renders and the denied-microphone note shows, the listening path with real audio NOT exercised (the automated tab cannot grant the mic — owed to a hand check); eval-10: 0 P0, 0 P1, 2 P2 → BACKLOG, working tree identical before / open / close; `npm run verify` green from a fresh reset: typecheck, lint, hooks 60/21+23/6+5, unit 171, DB 318, e2e 12 + 6 skipped. Hosted release 2026-09-12 07:25–07:40Z in the runbook's order: `db dump --linked` to backups/2026-09-12-f16 (data 38 KB), the dump restored locally on 0019 by the user's `psql` load and 0020 applied on top with every count matching the hosted project (RUNBOOK drill record), the gate shown with the three erased values named, the user ran `db push --linked`, `migration list --linked` 0020 remote, `db diff --linked` clean but for the platform's `ensure_rls`, the hosted vision read back with its goal / proof / deadline / obstacle kept and `picture` null. Then, at the user's word, commit d1902ae (pre-commit verify green: hooks, unit 171, DB 318, e2e 12) and `git push` at 17:14Z; Vercel deployment success at 17:14:31Z, the served stylesheet carries the F16 rules, deployed spec 4/4. The F15-code-on-0020-schema window lasted from the `db push` (~07:40Z) to the deploy (17:14Z) — the Vision tab errored for signed-in users in between; the sprint, Today and Insights paths did not read the dropped column) | 1h34m | 4 (three interview batches · the plan approval) | 2 (Dictate failed lint twice — setState in an effect, then a ref written during render — before `useSyncExternalStore` and the event-handler read; the first e2e run 404'd on every route because `next typegen` ran while Playwright's server was up, memory saved) | 0 |
| F17 — One-box capture: the parser, one situations library, delete, the cue note and why gone | 2026-09-12T18:01Z (the clock read when feature mode was decided; the row was written on plan approval because plan mode forbids edits) | 2026-09-12T20:00Z (migrations 0021 + 0022: one situations library, `delete_situation`, `create_situations`, `parse_permit`, `sprints.why` and `cues.explanation` dropped, `start_sprint` 22 → 20 arguments; `db reset` clean; `scripts/rehearse-0021.mjs` 22 assertions green on legacy-shaped rows; eight live mutations red and restored; `npm run verify` green: typecheck, lint, hooks 60/21+23/6+5, unit 203 + 3 skipped, DB 325, e2e 12 + 6 skipped; visual pass by Playwright: 48 captures, Dusk + Night × 1138 + 390, no overflow; eval-11: 0 P0, 0 P1, 4 P2 → one SPEC amendment, three BACKLOG, working tree identical before / open / close (md5); dictation with real audio and the stop → sort path NOT exercised — owed to the owner's hand check; the real model was exercised after the release: the live smoke passed 3 of 3 at 21:54Z with the owner's key in `.env`. Hosted release 2026-09-12 in the runbook's order: `db dump --linked` to backups/2026-09-12-f17 (data 39 KB), the dump restored locally on 0020 by the user's `psql` load and 0021 + 0022 applied on top with all thirteen counts matching and the 11 hosted situations renumbered 1..11 (RUNBOOK drill record 20:10), the gate shown with the one erased `why` named, the user's `db push --linked`, then in the same step `migration list --linked` (0021, 0022 remote) and `git push` at 21:46:54Z; `db diff --linked` clean but for the platform's `ensure_rls`; the hosted rows read back through the service role (ranks 1..11, no duplicate, three columns gone, counts intact, `day_offered_items` answering without a note); Vercel deployment success 21:47:27Z, the served stylesheet carries `.capture-box`, deployed spec 4/4. The F16-code-on-0021-schema window lasted about two minutes, from the push to the deploy — the commit was made before the gate so that only the push and the checks sat inside it) | 1h59m | 2 (the direction and the spec approvals; five interview batches) | 2 (a constant exported from the `"use server"` action took every page down under Playwright — `tsc` silent, FIX_LOG 2026-09-12 and a guard test; the delete outcome line sat in a row that unmounts, hoisted to page level) | 0 |
| F18 — Five-step wizard, confidence and mantra copy, the rail's Edit menus | 2026-09-12T22:08Z | 2026-09-12T22:54Z (direct build, no migration, no evaluator trigger; `components/wizard/{draft,StepArea,StepMeasure,StepConfidence,StepTargets,StepItems}` + `components/Menu.tsx`, the rail's two Edit menus, `OptionRow.ariaDisabled`, `StartSprintInput` without `intention`; `tests/unit/wizard.test.ts` 9 tests; e2e: five steps, Create new, the aria-disabled row, the DB read of 14 null intentions, the four rail flows, Escape and focus return; visual pass by Playwright: 32 captures, Dusk + Night × 1138 + 390, no overflow (`docs/mockups/f18-wizard-rail/`); four live mutations red and restored (unit · e2e · tsc · e2e); `npm run verify` green: typecheck, lint, hooks 60/21+23/6+5, unit 212 + 3 skipped, DB 325, e2e 12 + 6 skipped) | 0h46m | 0 | 1 (the dialog never returned focus to its opener under Strict Mode — two fixes on a wrong theory, the third on the double-run effect, FIX_LOG 2026-09-12) | 0 |
